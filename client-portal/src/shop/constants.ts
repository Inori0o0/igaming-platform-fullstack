/** 實體商品運費（VAC）；小計為 0 時不收運費。 */
export const PHYSICAL_SHIPPING_FEE_VAC = 60;

/** 購物車單一品項可持有的最大數量（頭像除外）。 */
export const MAX_LINE_QUANTITY = 99;

/** 加購面板單次可選的最大數量；小於 {@link MAX_LINE_QUANTITY}，避免一次大量加車。 */
export const MAX_ADD_BATCH_QUANTITY = 5;

/** 頭像每款在購物車內的上限。 */
export const AVATAR_MAX_QUANTITY = 1;

---
description: vAcAnt iGaming 專案的技術棧、架構與風格規範
globs: |
  client-portal/**/*.{ts,tsx,js,jsx}
  admin-dashboard/**/*.{ts,tsx,js,jsx}
alwaysApply: false
---

# vAcAnt iGaming 專案規則

## 技術棧與架構

- **前台 Client Portal**
  - 使用 Next.js 16 + React 19 + Tailwind CSS 4
  - 整合 Supabase（Auth、PostgreSQL、Storage、Realtime）
  - 狀態管理優先用 Zustand
  - 動畫優先使用 Framer Motion
- **管理後台 Admin Dashboard**
  - 使用 Vite + React 19
  - 圖表使用 Recharts
- **路由與模組結構**
  - 依計畫中的路由結構拆分頁面與模組（games、shop、wallet、profile 等）
  - Next.js App Router 建議用 Route Groups 分層：
    - `(lobby)`：主站頁面（套 `ClientLayoutShell`：Header/Sidebar/Footer）
    - `(auth)`：純流程頁（例如 `/auth/callback`），**不套主站殼**
  - `/profile/*` 採 nested layout：在 `profile/layout.tsx` 放 tabs/共用 UI
  - 共用元件（按鈕、卡片、Modal、Input、Avatar、Loading 等）集中在可重用的 `components` 資料夾

## 資料與 Supabase

- 依 plan 內定義的資料表（users、wallets、transactions、game_history、achievements、products、orders 等）設計型別與 API
- 優先透過 Supabase Client 存取資料，不在前端硬編假資料
  - 例外：在「路由骨架 / 版位確認」階段可用 placeholder/demo 資料，但要清楚標示，並規劃替換為真資料的下一步
- 交易、遊戲紀錄、訂單等與金流相關資料要有清楚的型別與狀態（pending/success/failed 等）

## UI / UX 風格

- 整體主題為 **深色 + 霓虹賭場感**，主色調以青色霓虹搭配品牌 vAcAnt 風格
- Italian Brainrot 宇宙角色（Tralalero Tralala、Tung Tung Tung Sahur、Bombardiro Crocodilo、Brr Brr Patapim、Lirili Larila、Elefanto Cactuso 等）：
  - 在 Slots、Blackjack、Baccarat、Lottery 等遊戲中共用同一套角色設定與美術資產
  - 不同遊戲的 UI 顏色與插畫保持同一世界觀與視覺語言
- Loading 與轉場：
  - 使用計畫中定義的 `SplashScreen`、`GameLoadingScreen`、`LogoLoader` 統一 Logo 載入動畫
  - 優先利用 Framer Motion 做淡入淡出、縮放與狀態過場
  - 避免過度 loading 干擾：
    - `SplashScreen` 以「首次進站」為主（可搭配 `minVisibleMs` 防閃爍）
    - 後續短暫同步/載入優先用小型 `LogoLoader` 或 skeleton，不用整頁覆蓋

## 體驗與功能優先順序

- 優先讓使用者盡快體驗到：
  - 首頁 + 至少一個完整可玩的老虎機遊戲
  - 基本錢包系統（餘額顯示 + 模擬充值/免費領取）
- 之後再逐步補齊：
  - 其他博弈遊戲（Blackjack、Baccarat、Lottery）
  - 商品系統與購物車
  - 個人中心、成就、訂單歷史
  - 管理後台儀表板與管理功能

## 通用開發原則

- 元件化與可重用性優先：遊戲卡片、商品卡片、列表、統計區塊等盡量抽成獨立元件
- 保持型別明確：遊戲類型、交易類型、成就條件等建立對應 enum 或 union type
- 所有動畫、特效、角色互動都以「可讀性與可用性」優先，其次才是華麗程度
